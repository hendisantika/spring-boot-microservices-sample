spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888

---

spring:
  cloud:
    config:
      uri: http://configserver:8888
  config:
    activate:
      on-profile:
