spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hendisantika/spring-boot-microservices
          # While in development mode, you can configure the config server to pick up configuration files from
          # the file system

#           uri: file://Users/hendisantika/Documents/Spring-Cloud/Trainings/Code Samples/Enterprise Java/Micro Services/sample-config

# Defines the port where the config server is running so that rest of the services can pick up
# their external configurations
server:
  port: 8888